---
title: "Speech-to-Text"
description: "Transcribe audio files to text using the OpenAI-compatible /v1/audio/transcriptions endpoint."
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

## Endpoint

```
POST /v1/audio/transcriptions
```

Transcribes audio to text using Groq's hosted Whisper model. The API is OpenAI-compatible — any client that works with OpenAI's audio transcriptions API can point to this gateway instead.

## Request

`multipart/form-data`

| Field | Type | Default | Description |
|---|---|---|---|
| `file` | file | required | Audio file to transcribe. Supported formats: mp3, mp4, wav, webm, m4a. |
| `model` | string | `whisper-large-v3-turbo` | Whisper model to use. |
| `language` | string | — | ISO-639-1 language code (e.g. `en`, `es`, `fr`). Improves accuracy when specified. |

## Response

```json
{
  "text": "The transcribed text appears here."
}
```

## Free Limits

- **2,000 requests/day** (Groq free tier)
- **8 hours of audio/day** (Groq free tier)

## Examples

<Tabs>
<TabItem label="curl">
```bash
curl https://your-gateway.workers.dev/v1/audio/transcriptions \
  -F file=@recording.mp3 \
  -F model=whisper-large-v3-turbo \
  -F language=en
```
</TabItem>
<TabItem label="JavaScript">
```js
const formData = new FormData();
formData.append('file', audioFile);
formData.append('model', 'whisper-large-v3-turbo');

const response = await fetch('https://your-gateway.workers.dev/v1/audio/transcriptions', {
  method: 'POST',
  body: formData,
});

const { text } = await response.json();
console.log(text);
```
</TabItem>
<TabItem label="OpenAI SDK">
```js
import OpenAI from 'openai';
import fs from 'fs';

const client = new OpenAI({
  baseURL: 'https://your-gateway.workers.dev/v1',
  apiKey: 'anything',
});

const transcription = await client.audio.transcriptions.create({
  file: fs.createReadStream('recording.mp3'),
  model: 'whisper-large-v3-turbo',
});

console.log(transcription.text);
```
</TabItem>
</Tabs>
